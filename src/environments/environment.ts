// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyB5mGMYSN41euqRFoVtNDcOnGDZIxOIJeI",
    authDomain: "imageupload-d68a0.firebaseapp.com",
    databaseURL: "https://imageupload-d68a0.firebaseio.com",
    projectId: "imageupload-d68a0",
    storageBucket: "imageupload-d68a0.appspot.com",
    messagingSenderId: "50181059782"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
